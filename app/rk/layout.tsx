
export const metadata = {
    title: 'çiçek',
    description: 'cicek',
    openGraph: {
        title: 'cicek',
        description: 'cicek',
    },
};

export default function RkLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return <>{children}</>;
}
